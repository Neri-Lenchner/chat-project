# אפיון פרונט – מערכת צ'אט

## 1. מטרת המערכת

מערכת צ'אט המאפשרת למשתמשים:

* להירשם למערכת.
* להתחבר באמצעות מספר טלפון.
* לצפות ברשימת השיחות שלהם.
* ליצור שיחה חדשה.
* למחוק שיחה קיימת.
* להיכנס לשיחה ולהציג את ההודעות.
* לשלוח הודעות למשתמש אחר.

---

# 2. מסך הרשמה

## 2.1 שדות

| שדה      | סוג   | חובה |
| -------- | ----- | ---- |
| שם פרטי  | טקסט  | כן   |
| שם משפחה | טקסט  | כן   |
| טלפון    | טלפון | כן   |

## 2.2 כפתור הרשמה

בלחיצה על כפתור **הרשמה**:

1. יש לבצע ולידציה לכל שדות החובה.
2. במידה והוולידציה תקינה, יש לבצע קריאה לשרת.
3. במהלך הקריאה יש להציג אינדיקציה לטעינה.
4. יש למנוע שליחה כפולה של הטופס בזמן הטעינה.

### API

**Endpoint:**

`POST http://localhost:8000/api/user/`

**Request Body:**

`User`

דוגמה:

```json
{
  "firstName": "ישראל",
  "lastName": "ישראלי",
  "phone": "0501234567"
}
```

**Response:**

`User`

### תרחיש הצלחה

* קבלת תשובה תקינה מהשרת.
* שמירת פרטי המשתמש לפי הצורך.
* מעבר למסך הראשי.

### תרחיש שגיאה

במקרה של שגיאה מהשרת:

* יש להפסיק את מצב הטעינה.
* יש להציג הודעת שגיאה מתאימה למשתמש.
* יש לאפשר ניסיון חוזר.

---

# 3. מסך התחברות

## 3.1 שדות

| שדה   | סוג   | חובה |
| ----- | ----- | ---- |
| טלפון | טלפון | כן   |

## 3.2 כפתור התחברות

בלחיצה על כפתור **התחברות**:

1. יש לבצע ולידציה למספר הטלפון.
2. יש לבצע קריאה לשרת.
3. יש להציג אינדיקציה לטעינה.
4. יש למנוע שליחה כפולה בזמן הטעינה.

### API

**Endpoint:**

`POST http://localhost:8000/api/user/login`

**Request Body:**

`Phone Number`

דוגמה:

```json
{
  "phone": "0501234567"
}
```

**Response:**

`User`

### תרחיש הצלחה

* קבלת משתמש מהשרת.
* שמירת פרטי המשתמש באפליקציה לפי הצורך.
* מעבר למסך הראשי.

### תרחיש שגיאה

במקרה של שגיאה:

* עצירת אינדיקציית הטעינה.
* הצגת הודעת שגיאה מתאימה.
* אפשרות לנסות להתחבר שוב.

---

# 4. מסך ראשי – רשימת שיחות

המסך הראשי יציג את כלל השיחות של המשתמש המחובר.

## 4.1 תפריט

במסך הראשי יוצג תפריט המאפשר גישה לאפשרויות הרלוונטיות של האפליקציה.

---

## 4.2 שליפת רשימת השיחות

### API

**Endpoint:**

`GET http://localhost:8000/api/room-user/room/{user-id}`

**Path Parameter:**

* `user-id` – מזהה המשתמש המחובר.

**Response:**

רשימת חדרים (`List<Room>`).

עבור כל שיחה יוצגו לפחות:

* שם איש הקשר.
* ההודעה האחרונה.
* תאריך/שעה של ההודעה האחרונה.

### הצגת תאריך

יש להציג את התאריך בהתאם לכללים הבאים:

* אם ההודעה האחרונה נשלחה **היום** – להציג את השעה בלבד.
* אם ההודעה אינה מהיום – להציג את התאריך.

לדוגמה:

```text
12:45
```

או:

```text
19/08/2026
```

---

# 5. מחיקת שיחה

יש לאפשר מחיקת שיחה באמצעות **לחיצה ארוכה** על השיחה.

## 5.1 התנהגות

1. המשתמש מבצע לחיצה ארוכה על שיחה.
2. נפתח Popup / Dialog.
3. מוצגת שאלה למשתמש:

> האם למחוק את השיחה?

4. יוצגו אפשרויות:

   * מחיקה.
   * ביטול.

## 5.2 מחיקת השיחה

בבחירה באפשרות **מחיקה**, יש לבצע:

### API

**Endpoint:**

`DELETE http://localhost:8000/api/room/{id}`

**Path Parameter:**

* `room-id` – מזהה החדר.

**Response:**

`204 No Content`

### לאחר הצלחה

* יש להסיר את השיחה מרשימת השיחות.
* אין צורך לבצע מעבר למסך אחר.

### במקרה של שגיאה

* יש להציג הודעת שגיאה מתאימה.
* השיחה תישאר ברשימה.

---

# 6. הוספת שיחה חדשה

במסך הראשי תהיה אפשרות להוסיף שיחה חדשה.

## 6.1 רשימת אנשי קשר

בשלב זה רשימת אנשי הקשר תהיה **רשימת Mock מקומית בתוך האפליקציה**.

עבור כל איש קשר יוצג:

* שם איש הקשר.

דוגמה:

```text
יוסי כהן
דני לוי
משה ישראלי
```

---

## 6.2 יצירת שיחה

לאחר בחירת איש קשר, יש ליצור חדר חדש.

### API

**Endpoint:**

`POST http://localhost:8000/api/room/`

**Request Body:**

`Room`

**Response:**

`Room`

### תרחיש הצלחה

לאחר קבלת תשובה מוצלחת מהשרת:

1. יש להוסיף את השיחה לרשימת השיחות.
2. יש לעבור באופן אוטומטי למסך השיחה הרלוונטית.
3. יש להציג את שם איש הקשר / שם החדר.

### תרחיש שגיאה

* הצגת הודעת שגיאה למשתמש.
* הישארות במסך הנוכחי.
* אפשרות לנסות שוב.

---

# 7. מסך שיחה

מסך השיחה יציג את כלל ההודעות בחדר שנבחר.

## 7.1 פרטי המסך

יש להציג:

* שם החדר / איש הקשר.
* רשימת ההודעות.
* תוכן כל הודעה.
* השעה שבה נשלחה ההודעה.
* שדה להזנת הודעה חדשה.
* כפתור שליחת הודעה.

---

# 8. שליפת הודעות בחדר

### API

**Endpoint:**

`GET http://localhost:8000/api/message/room/{id}`

**Path Parameter:**

* `room-id` – מזהה החדר.

**Response:**

רשימת הודעות (`List<Message>`).

### עבור כל הודעה יש להציג:

* תוכן ההודעה.
* שעת שליחת ההודעה.

דוגמה:

```text
שלום, מה נשמע?
12:45
```

---

# 9. שליחת הודעה

שליחת הודעה תתבצע באמצעות מזהה המשתמש השולח ומזהה המשתמש המקבל.

### API

**Endpoint:**

`POST http://localhost:8000/api/message/user/{user-id}/other/{other-user-id}`

**Path Parameters:**

* `user-id` – מזהה המשתמש המחובר.
* `other-user-id` – מזהה המשתמש שאליו נשלחת ההודעה.

**Request Body:**

תוכן ההודעה.

דוגמה אפשרית:

```json
{
  "content": "שלום, מה נשמע?"
}
```

**Response:**

`Message`

### תרחיש הצלחה

לאחר קבלת ההודעה מהשרת:

1. יש להוסיף את ההודעה לרשימת ההודעות.
2. יש לנקות את שדה הטקסט.
3. יש להציג את ההודעה במסך.
4. יש לגלול להודעה האחרונה במידת הצורך.

### תרחיש שגיאה

* הצגת הודעת שגיאה למשתמש.
* אין לנקות את תוכן ההודעה שהוזנה.
* יש לאפשר ניסיון שליחה חוזר.

---

# 10. סיכום API

| פעולה       | Method | Endpoint                                            | Request       | Response         |
| ----------- | ------ | --------------------------------------------------- | ------------- | ---------------- |
| הרשמה       | POST   | `/api/user/`                                        | User          | User             |
| התחברות     | POST   | `/api/user/login`                                   | Phone Number  | User             |
| קבלת שיחות  | GET    | `/api/room-user/room/{user-id}`                     | Path: user-id | List of Rooms    |
| יצירת שיחה  | POST   | `/api/room/`                                        | Room          | Room             |
| מחיקת שיחה  | DELETE | `/api/room/{id}`                                    | Path: room-id | 204              |
| קבלת הודעות | GET    | `/api/message/room/{id}`                            | Path: room-id | List of Messages |
| שליחת הודעה | POST   | `/api/message/user/{user-id}/other/{other-user-id}` | Message       | Message          |

---

# 11. ניווט בין המסכים

מבנה הניווט הבסיסי:

```text
מסך הרשמה
     │
     ▼
מסך ראשי
     │
     ├──────────────► הוספת שיחה
     │                     │
     │                     ▼
     │                 מסך שיחה
     │
     └──────────────► מסך שיחה
```

בנוסף:

```text
מסך התחברות
     │
     ▼
מסך ראשי
     │
     ▼
מסך שיחה
```

---

# 12. מצבי טעינה

בכל פעולה מול השרת יש להציג מצב טעינה מתאים.

הפעולות הרלוונטיות:

* הרשמה.
* התחברות.
* טעינת רשימת השיחות.
* יצירת שיחה.
* מחיקת שיחה.
* טעינת הודעות.
* שליחת הודעה.

בזמן פעולה קריטית, יש למנוע ביצוע כפול של אותה פעולה.

---

# 13. טיפול בשגיאות

הפרונט צריך לטפל בשגיאות API בצורה אחידה.

בכל שגיאה:

1. יש לעצור את מצב הטעינה.
2. יש להציג הודעה ידידותית למשתמש.
3. אין להציג למשתמש Stack Trace או מידע טכני.
4. יש לאפשר ניסיון חוזר כאשר הפעולה מאפשרת זאת.

דוגמאות להודעות:

```text
אירעה שגיאה. נסה שוב.
```

```text
לא ניתן להתחבר. בדוק את מספר הטלפון ונסה שוב.
```

```text
לא ניתן לטעון את השיחות. נסה שוב.
```

```text
לא ניתן לשלוח את ההודעה. נסה שוב.
```

---

# 14. ולידציה

## הרשמה

יש לוודא שכל השדות הבאים מלאים:

* שם פרטי.
* שם משפחה.
* טלפון.

## התחברות

יש לוודא שמספר הטלפון מולא.

## שליחת הודעה

אין לאפשר שליחה של הודעה ריקה.

---

# 15. מודלים עיקריים

המערכת עושה שימוש במספר מודלים מרכזיים:

## User

```text
User
├── id
├── firstName
├── lastName
└── phone
```

## Room

```text
Room
├── id
└── ...
```

## Message

```text
Message
├── id
├── content
├── ...
└── ...
```

> השדות המדויקים של `User`, `Room` ו־`Message` צריכים להיות מותאמים למודלים בפועל של השרת.

---

# 16. דרישות UX כלליות

* ממשק ברור ופשוט.
* אינדיקציה ברורה כאשר מתבצעת פעולה מול השרת.
* הודעות שגיאה ידידותיות.
* מניעת שליחה כפולה.
* שמירה על גלילה להודעה האחרונה במסך השיחה.
* אישור לפני מחיקת שיחה.
* הצגת השעה/תאריך של הודעות בהתאם לכללי התאריך שהוגדרו.
* לאחר יצירת שיחה חדשה יש להעביר את המשתמש ישירות לשיחה.

---

# 17. תרחיש משתמש מלא

### משתמש חדש

1. המשתמש נכנס למסך הרשמה.
2. ממלא שם פרטי, שם משפחה ומספר טלפון.
3. לוחץ על "הרשמה".
4. הפרונט שולח `POST /api/user/`.
5. לאחר הצלחה המשתמש מועבר למסך הראשי.
6. במסך הראשי נטענת רשימת השיחות.
7. המשתמש בוחר "הוספת שיחה".
8. מוצגת רשימת אנשי קשר מקומית.
9. המשתמש בוחר איש קשר.
10. נשלחת בקשה ליצירת חדר.
11. לאחר הצלחה המשתמש מועבר למסך השיחה.
12. נטענות ההודעות הקיימות.
13. המשתמש יכול לשלוח הודעות חדשות.

### משתמש קיים

1. המשתמש נכנס למסך התחברות.
2. מזין מספר טלפון.
3. לוחץ על "התחברות".
4. הפרונט שולח `POST /api/user/login`.
5. לאחר הצלחה המשתמש מועבר למסך הראשי.
6. רשימת השיחות נטענת.
7. המשתמש יכול להיכנס לשיחה, לשלוח הודעה או למחוק שיחה.

---

# 18. נקודות שדורשות השלמה מול ה-Backend

לפני מימוש מלא מומלץ לוודא מול צד השרת:

* המבנה המדויק של `User`.
* המבנה המדויק של `Room`.
* המבנה המדויק של `Message`.
* המבנה המדויק של Request ליצירת הודעה.
* האם `room` מייצג שיחה בין שני משתמשים בלבד או תומך בקבוצה.
* כיצד מזהים את המשתמש המחובר בפרונט.
* האם נדרש מנגנון Authentication / Token.
* מהו מבנה השגיאות שמוחזר מהשרת.
* האם קיימת מגבלה או ולידציה על מספר הטלפון.
* מהו שם השדה המדויק עבור תוכן הודעה.
* מהו שם השדה המדויק עבור שם איש הקשר.
* האם לאחר יצירת Room השרת מחזיר את המשתמשים המשויכים לחדר.

---

# 19. Checklist למימוש פרונט

## הרשמה

* [ ] יצירת מסך הרשמה.
* [ ] שדה שם פרטי.
* [ ] שדה שם משפחה.
* [ ] שדה טלפון.
* [ ] ולידציה.
* [ ] חיבור ל־API.
* [ ] מצב טעינה.
* [ ] טיפול בהצלחה.
* [ ] טיפול בשגיאה.
* [ ] ניווט למסך הראשי.

## התחברות

* [ ] יצירת מסך התחברות.
* [ ] שדה טלפון.
* [ ] ולידציה.
* [ ] חיבור ל־API.
* [ ] מצב טעינה.
* [ ] טיפול בהצלחה.
* [ ] טיפול בשגיאה.
* [ ] ניווט למסך הראשי.

## מסך ראשי

* [ ] תפריט.
* [ ] טעינת רשימת שיחות.
* [ ] הצגת שם איש קשר.
* [ ] הצגת הודעה אחרונה.
* [ ] הצגת שעה/תאריך.
* [ ] לחיצה על שיחה.
* [ ] לחיצה ארוכה על שיחה.
* [ ] Popup מחיקה.
* [ ] חיבור ל־API מחיקת שיחה.
* [ ] רענון הרשימה לאחר מחיקה.
* [ ] כפתור הוספת שיחה.
* [ ] רשימת אנשי קשר Mock.
* [ ] יצירת Room.
* [ ] מעבר לשיחה לאחר יצירה.

## מסך שיחה

* [ ] הצגת שם החדר.
* [ ] טעינת הודעות.
* [ ] הצגת תוכן ההודעה.
* [ ] הצגת שעה.
* [ ] שדה הזנת הודעה.
* [ ] כפתור שליחה.
* [ ] שליחת הודעה לשרת.
* [ ] מצב טעינה בשליחה.
* [ ] הצגת הודעה חדשה.
* [ ] ניקוי שדה ההודעה לאחר הצלחה.
* [ ] גלילה להודעה האחרונה.
* [ ] טיפול בשגיאות.
